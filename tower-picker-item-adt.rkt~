
(define (make-tower-picker-item tower-image tower-name)
  

  (define (dispatch msg)
    (cond ((eq? msg 'get-name) tower-name)
          ((eq? msg 'get-image) tower-image)
          ((eq? msg 'get-position) position)))
  dispatch)